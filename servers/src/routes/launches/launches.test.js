const app = require();

describe("Test GET /Launch", () => {
  test("it respond with status 200", () => {
    const response = 200;
    expect(response).toBe(200);
  });
});

describe("Test POST /Launch", () => {
  test("it respond with status 200", () => {});
  test("it should catch missing required properties", () => {});
  test("i shosuld detect invalid date", () => {});
});
